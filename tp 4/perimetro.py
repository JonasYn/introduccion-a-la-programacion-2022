#Cree un script que muestre en pantalla el perímetro de un rectángulo,
#leyendo su base y altura desde teclado.
#erímetro = 2 * (base + altura)

base = int(input(f"ingrese la base del rectangulo"))
altura = int(input(f"ingrese la altura del rectangulo"))
perimetro = (base + altura) * 2
print (perimetro)